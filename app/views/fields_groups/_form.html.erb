<%= form_for(@fields_group) do |f| %>
  <% if @fields_group.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@fields_group.errors.count, "error") %> prohibited this fields_group from being saved:</h2>

      <ul>
      <% @fields_group.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :id %><br />
    <%= f.number_field :id %>
  </div>
  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :description %><br />
    <%= f.text_field :description %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
