<%= form_for(@application_user) do |f| %>
  <% if @application_user.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@application_user.errors.count, "error") %> prohibited this application_user from being saved:</h2>

      <ul>
      <% @application_user.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
