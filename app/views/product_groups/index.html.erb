<h1>Listing product_groups</h1>

<table>
  <tr>
    <th>Id</th>
    <th>Name</th>
    <th>Description</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @product_groups.each do |product_group| %>
  <tr>
    <td><%= product_group.id %></td>
    <td><%= product_group.name %></td>
    <td><%= product_group.description %></td>
    <td><%= link_to 'Show', product_group %></td>
    <td><%= link_to 'Edit', edit_product_group_path(product_group) %></td>
    <td><%= link_to 'Destroy', product_group, :confirm => 'Are you sure?', :method => :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Product group', new_product_group_path %>
