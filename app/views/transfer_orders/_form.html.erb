<%= form_for(@transfer_order) do |f| %>
  <% if @transfer_order.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@transfer_order.errors.count, "error") %> prohibited this transfer_order from being saved:</h2>

      <ul>
      <% @transfer_order.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :id %><br />
    <%= f.number_field :id %>
  </div>
  <div class="field">
    <%= f.label :orderNumber %><br />
    <%= f.number_field :orderNumber %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
