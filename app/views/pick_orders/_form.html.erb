<%= form_for(@pick_order) do |f| %>
  <% if @pick_order.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@pick_order.errors.count, "error") %> prohibited this pick_order from being saved:</h2>

      <ul>
      <% @pick_order.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :id %><br />
    <%= f.number_field :id %>
  </div>
  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :description %><br />
    <%= f.text_field :description %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
