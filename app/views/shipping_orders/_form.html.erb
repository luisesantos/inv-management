<%= form_for(@shipping_order) do |f| %>
  <% if @shipping_order.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@shipping_order.errors.count, "error") %> prohibited this shipping_order from being saved:</h2>

      <ul>
      <% @shipping_order.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :id %><br />
    <%= f.number_field :id %>
  </div>
  <div class="field">
    <%= f.label :orderNumber %><br />
    <%= f.text_field :orderNumber %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
